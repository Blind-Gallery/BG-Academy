table:
  name: level_catalog
  schema: public
is_enum: true
array_relationships:
  - name: courses
    using:
      foreign_key_constraint_on:
        column: level
        table:
          name: courses
          schema: public
